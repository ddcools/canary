<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">Canary</a>
    </div>
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-left">
        <li><%= link_to "Riders", riders_path, :via => "get" %></li>
        <li><%= link_to "Places", places_path, :via => "get" %></li>
        <li><%= link_to "Rides", rides_path, :via => "get" %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <%  if @show_search_form %>
        <li>
          <%= form_tag(root_path, :method => "get", id: "search_form" , class: "navbar-form") do %>
          <%= text_field_tag :search_query, nil, placeholder: "Search Riders", class: "form-control" %>
          <%= submit_tag "Search", class: "btn btn-default" %>
          <% end %>
        </li>
        <% end %>

        <% if current_user %>
          <li><%= link_to "Logout", logout_path, :via => "get" %></li>
        <% else %>
          <li><%= link_to "Login", login_path, :via => "get" %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>